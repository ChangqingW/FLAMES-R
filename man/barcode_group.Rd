% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_barcode.R
\name{barcode_group}
\alias{barcode_group}
\title{Create a Flexiplex barcode group}
\usage{
barcode_group(name, bc_list_name, max_edit_distance = 2)
}
\arguments{
\item{name}{The group identifier. Must match the \code{group} argument of every
\code{\link{barcode_segment}} that belongs to this group.}

\item{bc_list_name}{A key used to look up the allow-list file for this group from
the \code{barcodes_files} argument of \code{\link{find_barcode}}. The key must
match a name in \code{barcodes_files}.}

\item{max_edit_distance}{Non-negative integer. Maximum total edit distance allowed
when matching the concatenated segment sequences against the group allow-list.
Default: \code{2}.}
}
\value{
A \code{FlexiplexGroup} object for use in \code{\link{find_barcode}}.
}
\description{
Creates a \code{FlexiplexGroup} object that links together a set of
\code{MATCHED_SPLIT} \code{\link{barcode_segment}}s for concatenatiing segments before barcode
matching. A barcode group lets flexiplex jointly match multiple segment sequences
against a shared allow-list (e.g. the concatenation of two barcode halves).
}
\seealso{
\code{\link{barcode_segment}}, \code{\link{find_barcode}}
}
